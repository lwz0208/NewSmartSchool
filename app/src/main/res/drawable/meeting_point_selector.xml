<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="false"><shape android:shape="rectangle">

        <!-- 填充的颜色 -->
        <solid android:color="@color/primary_green"></solid>
        <!-- 设置按钮的四个角为弧形 -->
        <!-- android:radius 弧形的半径 -->
        <corners android:radius="18dip" />
        <padding android:left="35dp" android:right="35dp" android:top="8dp" android:bottom="8dp" />
    </shape></item>
    <item android:state_pressed="true"><shape android:shape="rectangle">

        <!-- 填充的颜色 -->
        <solid android:color="@color/primary_green_dark"></solid>
        <!-- 设置按钮的四个角为弧形 -->
        <!-- android:radius 弧形的半径 -->
        <corners android:radius="18dip" />
        <padding android:left="35dp" android:right="35dp" android:top="8dp" android:bottom="8dp" />
    </shape></item>

</selector>